no this is wrong it should show in space too, you are using the wrong model for where the atomosphere should be. actually just use the normal atompsohere coloring algorithm. dont use pathtracing just normal 1 pass raster for color. assume the sun is at noon over the us
